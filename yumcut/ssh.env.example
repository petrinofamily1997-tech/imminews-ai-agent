# Copy this file to .ssh.env and fill in the values below.
# The project download script reads from .ssh.env to run scp.
SSH_USERNAME=your-ssh-username
# Leave blank to be prompted at runtime (or rely on SSH keys)
SSH_PASSWORD=
SSH_SERVER_IP=203.0.113.10
SSH_REMOTE_BASE_PATH=/remote/server
SSH_LOCAL_BASE_PATH=/local/target
# Optional: uncomment to override the default SSH port (22)
# SSH_PORT=22
# Optional: set to true to force an interactive password prompt
# SSH_PROMPT_PASSWORD=true
